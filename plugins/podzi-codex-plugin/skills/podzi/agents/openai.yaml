interface:
  display_name: "Podzi Core Bridge"
  short_description: "Prepare window.podzi_cli from an existing Chrome Podzi tab."
  default_prompt: "Prepare the current Chrome Podzi tab for Podzi CLI access."
